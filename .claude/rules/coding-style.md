---
description: コーディングスタイルとファイル構成のルール
globs: "**/*.{ts,tsx}"
---

# コーディングスタイル

- イミュータビリティを優先: `const` を使い、`let` は最小限に。配列・オブジェクトの破壊的変更を避ける
- 1ファイル200行以下を目安にする。超える場合はコンポーネントやユーティリティに分割を検討
- 未使用のインポート、変数、関数を残さない
- `any` 型は使わない。型が不明な場合は `unknown` を使い、型ガードで絞り込む
- エラーハンドリングではユーザーに意味のあるメッセージを返す
